{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://owndesk.local/schemas/server/relay.json",
  "type": "object",
  "required": ["kind", "to", "envelope"],
  "properties": {
    "kind": { "const": "relay" },
    "to": { "$ref": "https://owndesk.local/schemas/common.json#/$defs/deviceId" },
    "envelope": { "$ref": "https://owndesk.local/schemas/envelope.json" }
  }
}
